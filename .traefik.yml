displayName: Enforce Lowercase URL HC
type: middleware
import: github.com/bchangiphc/lowercasehc
summary: If the URL path has any uppercase characters redirect the user to the lowercase equivalent.
testData: #no test data needed
  unused: var
